/**
 * 大致思路：后端连接池思想
 */

// 创建可回收节点
const createComponent = () => {};

// 首次创建一屏节点

/**
 * 头部观察者
 * 触发，向上滚动，已经标记过，直接去连接池里面取。
 * 移除可视区，则进行节点回收，丢入连接池。
 */

/**
 * 中间内容部分
 */

/**
 * 尾部观察者
 * 触发，向下滚动，已标记过，直接读取，未标记则去连接池里取万能模板，初始化获取高度，不符合，则重新生成节点，万能模板丢入连接池
 */